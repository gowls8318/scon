package com.scon.project.classfile.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.MessageSource;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;

import com.scon.project.classfile.service.ClassFileService;

import lombok.extern.slf4j.Slf4j;

@Slf4j // log테스트
@Controller
@RequestMapping("/admin")
public class ClassFileController {
	
	
	/*
	 * private ClassFileService classFileService; private MessageSource
	 * messageSource;
	 * 
	 * @Autowired public ClassFileController(ClassFileService classService,
	 * MessageSource messageSource) { this.classFileService = classFileService;
	 * this.messageSource = messageSource; }
	 */
	 
	

}
